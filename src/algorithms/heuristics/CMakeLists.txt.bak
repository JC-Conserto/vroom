set(private_src
    "${CMAKE_CURRENT_SOURCE_DIR}/heuristics.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/heuristics.h"
)

target_sources(vroom PRIVATE ${private_src})
target_sources(libvroom PRIVATE ${private_src})
